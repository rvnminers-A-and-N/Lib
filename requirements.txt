### non python requirements
## ipfs version 0.14.0 - 0.17.0

# datamanager
# cryptography==44.0.1
# python-evrmorelib -U

setuptools>=68.0.0  # Updated for Python 3.12 compatibility (distutils removed)

## needed for engine - Updated for Python 3.12 compatibility
pandas>=2.0.0  # 1.5.2 has no Python 3.12 wheel
joblib>=1.2.0
numpy>=1.24.0

## needed for data management - Updated for Python 3.12 compatibility
pyarrow>=10.0.1
fastparquet>=2022.12.0

## needed for configuration management
PyYAML>=6.0.1  # 6.0 has Python 3.12 issues

## needed for getting system data
psutil>=5.9.0

## needed for wallet
python-ravencoinlib==0.2.2
python-evrmorelib
mnemonic==0.20
base58==2.1.1
#qrcode==7.3.1

## needed for signing messages for connection with Satori server
#Crypto==1.4.1

## needed for Satori pubsub connection
websocket-client==1.4.2 # not sure if we need this
#websockets==10.4 # import websocket; websocket.WebSocket()
websockets==15.0.1 # import websocket; websocket.WebSocket()

# needed for encryption for wallet vault
pycryptodome==3.20.0

# needed for ethereum wallet functions
eth-account==0.13.7
eth-keys==0.6.1
web3==7.12.1

# for api validation
marshmallow==3.22.0

# for data manager
cryptography==44.0.2

# for validation
ipaddress==1.0.23

# for centrifugo
centrifuge-python==0.4.1

# for telemetry logging
requests>=2.31.0
sanic>=23.6.0
aiosqlite>=0.19.0
uvloop>=0.19.0

# P2P networking support (optional - enables hybrid/p2p modes)
satorip2p>=1.0.0